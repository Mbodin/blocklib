
(library
	(public_name blocklib)
	(name blocklib)
	(libraries libutils unix extlib yojson re uutf lwt ocamlgraph)
	(preprocess (pps lwt_ppx ppx_deriving ppx_inline_test))
	(inline_tests)
	(modes byte native))

(env (dev (flags (:standard -w -50))))

